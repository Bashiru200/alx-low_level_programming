#include <stdio.h>
/**
 * main - the main entry point
 *
 * Return: this function returns 0
 */
int main(void)
{
	int sdigit = 0;

	if (sdigit <= 9; ++sdigit)
	{
		printf("%d", sdigit);
		printf("\n");
	}
	return (0);
}
